const operation = {
    add(n1: number, n2: number): number {
        return n1 + n2;
    },
    substract: function(n1: number, n2: number): number {
        return n1 - n2;
    },
    multiply: (n1: number, n2: number) : number => n1 * n2,
};
