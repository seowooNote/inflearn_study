값
    ; 기본 자료형
        ; 숫자, 문자, 논리, undefined, null, symbol
    ; 참조 자료형
        ; 배열, 객체, 함수

    ; let variables : 타입 = 값;
    
빈 객체
    ; undefined 와 null 을 제외한 모든 값을 허용
        ; 모든 값이 Object 메서드를 상속 받음

진정한 빈 객체
    ; let emptyObject: Record<string, never> = {};

튜플
    ; 다르게 타입이 섞인 배열
        ; 제너릭으로 지정불가 <>

옵셔널 속성(?)
    ; 필수값을 선택적이게 할 수 있음