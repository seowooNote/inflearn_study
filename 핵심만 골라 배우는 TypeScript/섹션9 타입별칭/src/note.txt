타입 별칭(Type Alias)
    ; 타입에 다른 이름이나 대체 이름을 붙이는 것
        ; type AliasName = Type;
    ; 타입별칭의 효과
        ; 1. 의미를 보다 정확하게 전달할 수 있음
        ; 2. 재사용성 증가
        ; 3. 유지보수가 쉬워짐
        ; 4. 뛰어난 확장성
    ; 인터페이스 타입 병합 및 상속이 되지 않음